worker_processes 1;
events { worker_connections 1024; }
http {
    include mime.types;
    sendfile on;
    server {
        listen <%= ENV["PORT"] %>;
        root /app;
        index index.html;
    }
}
